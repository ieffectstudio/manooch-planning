Claude, we need to fix a set of UI alignment issues, a data structure bug, and a critical backend database crash related to the Order Detail flow. Please review the following three issues and plan the fixes.

1. Data Structure Bug (selectedAttributes)
Currently, the data structure for selectedAttributes is incorrect or bloated. You must normalize and strictly enforce its structure across the application so that it is always an array of objects containing only the ID and the value.

Required Format: [{ id, value }]

2. UI Alignment (Storefront vs. Admin)
The Storefront Order Detail page is missing the detailed list of order items.

The Fix: Below the "Order Tracking" section on the Storefront Order Detail page, you must implement the exact same Order Items List view that is currently used on the Admin Order Detail page.

Execution Order: You must first fix the selectedAttributes data structure in the Admin view. Once that is working perfectly, mirror that fixed Order Items view to the Storefront.

3. Backend Crash (QueryFailedError)
We are experiencing a 500 Internal Server Error in the backend when fetching order details.

The Error: QueryFailedError: column "name" does not exist at OrdersService.buildOrderDetail (Postgres / TypeORM).

The Fix: This indicates a mismatch between the entity properties being selected (or joined via relations) and the actual database schema. You need to inspect OrdersService.buildOrderDetail and correct the query builder or repository call so it stops requesting a non-existent "name" column.

Please acknowledge these three specific requirements and let me know which files you will investigate and modify (Frontend Admin components, Frontend Storefront components, and the NestJS OrdersService) to implement these fixes. Do not write the code yet; just confirm the plan.