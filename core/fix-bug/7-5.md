Claude, we have a series of UI state, synchronization, and display bugs across the Product Detail Page (PDP) and the Customer Order Detail Page. Please review the following issues and confirm your plan to fix them without breaking the existing architecture.

1. Customer Order Detail Page (Order Tracking & Items)

Stepper State: When an order's status reaches "Delivered" (تحویل داده شده), the visual order tracking stepper must be completely green, and all steps must be marked as checked/completed.

Missing Data on Line Items: When a customer views an order with multiple variants (e.g., Order ID: 5eca1bd0-58f7-4f48-9e6d-a6b4c1920243), the line items are displaying a price of "0" and the selected variants are completely hidden. The backend unit pricing logic is correct; this is strictly a frontend display issue. Fix the UI to correctly map and display the item prices and the selected variant attributes, mirroring the fix that was previously required on the Admin Order DP.

2. PDP Attribute Display (Color Palette)

Color Chips: In the PDP, when displaying color attributes (like in the "افزودن ویژگی کالا" section), do not just show the text name of the color. You must render a visual color chip next to the name.

Styling: Do not use checkboxes for this. Use the exact following HTML structure and style for the chip, injecting the correct hex/rgb value dynamically:
<div class="shrink-0 rounded-full border border-border-light" style="background-color: rgb(0, 0, 0); width: 14px; height: 14px;"></div>

3. PDP Sticky CTA & Variant State Sync

Variant Context Switching (Bug): When attribute pricing is enabled (e.g., Product ID: fc8aa838-8c27-4e96-b9c4-7796487f02ed), the Sticky CTA counter is not reacting to the currently selected variant. If a user adds 2 Red pencils to the cart, then switches their selection to the Black pencil, the CTA still shows "2". It must immediately drop to "0" (or the actual cart quantity of the Black pencil) to reflect the active variant selection.

Global Cart Sync on Return (Bug): If a user has 1 Black pencil in their cart, leaves the page, and comes back to the PDP, the Sticky CTA incorrectly resets and shows the default "Add to Cart" (افزودن به سبد) button. It must read the global cart state on mount and correctly display the counter (1).

4. PDP Unit Pricing Mode Issues

Cart Topbar Badge Calculation (Bug): When unit pricing is active (e.g., Product ID: d28afbc8-eb5c-42f8-884c-a6945bd4fdb6), the top bar cart badge is incorrectly summing the total sub-units instead of the order line items. For example, if a user adds 2 packages and 3 kilograms, it creates 2 line items in the cart (which is correct), but the badge shows "5". Update the badge logic to strictly count the number of order line items, so it correctly shows "2".

State Preservation: When a user navigates away and comes back to a unit-pricing PDP, the UnitBox must remember and display the previously selected units/items.

CTA Behavior in Unit Mode: In Unit Mode, the button next to the UnitSelectionChips (Add to Cart) must NOT show a quantity counter. Instead, clicking it should simply submit the data and directly redirect the user to the Cart page.

Please acknowledge these specific bugs and tell me which components/files you will modify to fix the Stepper, Order Items, Color Chips, and PDP State Sync. Do not write the code yet; just confirm the plan.