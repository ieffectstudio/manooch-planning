Claude, we have a batch of UI/UX styling bugs and a missing feature that need to be resolved across the frontend. Please review these descriptive requirements and implement the fixes without breaking the existing architecture.

1. Taxonomy Management (Missing Feature)

Issue: The taxonomy feature currently only allows users to "Create" or "Delete" items.

Requirement: Introduce an "Edit" capability. Users must be able to modify an existing taxonomy entry directly without being forced to delete and recreate it.

2. SectionNav Component (manooch-fronts\apps\portal\components\SectionNav\SectionNav.tsx)

Sticky Scroll Bug: The navigation bar is failing to stick to the top of the viewport when scrolling. It must remain pinned and visible at all times as the user scrolls down the page.

Active/Focused State Clarity: The visual distinction for the actively selected or focused item is currently too weak. Enhance the styling so the user can instantly recognize which section they are currently viewing.

Group Label Styling: The typography and layout for the group.label text look unpolished. Update its styling to give it a clean, distinct visual hierarchy that clearly marks it as a category header.

3. Ticketing System (Admin Page)

Issue: The TicketReplyBox is not anchored correctly.

Requirement: The reply box must be rigidly fixed (sticky) to the absolute bottom of the admin screen. The user must always have access to the input field, regardless of how far up or down they scroll in the ticket thread.

/human: Ensure the enhanced active states, typography updates, and sticky behaviors feel intuitive, accessible, and natural for a user's daily workflow.
/blindspot: Watch out for z-index collisions when making the SectionNav and TicketReplyBox sticky. Also, ensure the new Taxonomy "Edit" flow handles state updates gracefully without breaking existing data bindings or requiring a hard page refresh.