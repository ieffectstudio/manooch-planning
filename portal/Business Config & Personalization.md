Context: Phase 3 of the Super Admin PRD. We need to add "Group 3: تنظیمات کسب‌وکار و شخصی‌سازی" (Business & Personalization Settings) to the new Store Edit page layout we just built.

Tasks (Build these sections in the UI and connect them to save to the store's data):
1. تنظیمات کسب‌وکار (Business Settings): Build forms to view/override Tax config (rate, ID), Invoice settings (prefix, auto-numbering, footer), Store policies, Working hours, Currency, and Min order amount.
2. شخصی‌سازی (Personalization): Build UI to view/override Theme selection, Color palette, Font, Homepage layout, Custom CSS, Favicon, and Social media links.
3. سئو (SEO): Build read-only/edit views for Meta title/desc, Open Graph, GA tracking ID, Search Console verification, and Sitemap/Robots.txt.
4. اعلان‌ها (Notifications): Build toggles and preview UI for SMS, Email, and Push notification settings.

Strict Rule: These settings must reflect and override what the seller configures in their own panel. Keep the UI consistent with the new vertical layout. Confirm when complete.
