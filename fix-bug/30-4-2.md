Here is the full combined prompt:

---

I need help fixing several infrastructure, configuration, and middleware issues in my project (Next.js frontend + API backend).

Please analyze and propose clean, production-safe solutions for the following:

---

### 1️⃣ Admin routing structure

- I removed `/admin` from the beginning of admin routes.
- Now admin pages live at the **root of the admin application**.
- Some parts of the app are still behaving as if `/admin` exists.

✅ Fix routing so:
- Admin works correctly at root.
- No broken redirects.
- No hardcoded `/admin` assumptions remain.
- Middleware (if any) still protects admin routes properly.

---

### 2️⃣ Subdomain not working (localhost & IP)

Subdomain-based shops should work in:

- `localhost`
- Local IP (e.g., `192.168.x.x`)
- Production domain

✅ It must work in:
- `shop1.localhost`
- `shop1.192.168.x.x`
- And real production domain

Explain:
- Required DNS setup (dev & prod)
- Required Next.js config
- Required backend config
- Cookie domain considerations
- Any host header handling needed

---

### 3️⃣ Fix frontend environment variables

Frontend `.env` is not correctly configured.

✅ Fix:
- API base URL
- Admin base URL
- Public environment variables
- Dev vs production separation
- Ensure correct usage of `NEXT_PUBLIC_*`

Explain best practices and safe structure.

---

### 4️⃣ Fix CORS error when accessing API via IP

When accessing frontend from local IP (not localhost), API requests fail due to CORS.

✅ Fix:
- Proper CORS configuration
- Allow localhost + local IP + production domain
- Avoid wildcard security issues
- Handle credentials correctly (if using cookies)

Explain backend-side fix clearly.

---

### 5️⃣ Complete page shop link issue

On the "complete" page:

- Do NOT show `shopLink` directly.
- In development mode:
  - Show link as: `shop.name.ip:port`
- In production:
  - Show proper domain.
- Add button text: **"دیدن فروشگاه"**
- Ensure link works correctly in both environments.

---

### 6️⃣ Sync storefront middleware with all changes

The storefront middleware must be updated to reflect all the above changes:

- Subdomain extraction must work for both `localhost` and local IP (e.g., `192.168.x.x`).
- Middleware should correctly resolve the shop from subdomain in all environments (dev, local IP, production).
- Route rewriting and redirecting logic must be consistent with the new routing structure (no `/admin` prefix assumptions).
- Environment variables used inside middleware must follow the fixed `.env` structure.
- Middleware should gracefully handle:
  - Missing or invalid subdomains.
  - Unknown shops.
  - Dev vs production environment differences.
- Cookie domain and auth checks inside middleware must work across localhost and IP.

✅ Ensure middleware is the single source of truth for:
- Shop resolution.
- Route protection.
- Environment-aware behavior.

Do not duplicate logic that already exists elsewhere.

---

### General Requirements

- Do not over-engineer.
- Provide clean production-ready solutions.
- Separate dev vs prod logic clearly.
- Mention security considerations where relevant.
- Explain architecture decisions briefly.
- Changes must be consistent and in sync across all parts (middleware, env, routing, CORS, frontend).

---

### Assumptions

- Next.js frontend (admin app + storefront app)
- Node/Nest backend
- Subdomain-based multi-tenant architecture
- Cookie-based auth
- Dev environment uses both `localhost` and local IP

---

other bug in the admin business linkdooni tab no need to live/stay buttomsheet
each one create saved & each one edit save