Claude, we are upgrading the Manooch Cart, PDP, and Seller Dashboard logic to a highly robust, Variant-Aware and Unit-Aware architecture. You need to analyze the prototype files in C:\Sanji\Manooch-Deps\manooch-planings\cart-flow\modules\ and output the final, production-ready Tailwind/HTML prototypes to C:\Sanji\Manooch-Deps\manooch-planings\cart-flow\final.

Execute the following strict requirements step-by-step:

PHASE 1: Product Card & PDP State Logic (Storefront)

Product Card: Completely remove the "Add to Cart" CTA from the standard product card. The entire card must act as a clickable route to the PDP.

PDP Sticky CTA Matrix: Implement a pure function approach for the Sticky CTA. The logic must strictly follow:

Check if isSpecialProduct is true -> Override all and set CTA to "Call/Phone".

Check if isPriceInquiry is enabled AND the personalized shop config allows adding to cart.

The CTA must dynamically swap between a standard "Add to Cart" button and the Counter [-][x][+] based on the exact active variant.

Variant-Aware Selection: If the user selects the "Red" color chip, and adds it to the cart (counter becomes 1). When they click the "Green" color chip, the Sticky CTA MUST reset to the "Add to Cart" button, because Green is not in the cart. The cart must store these as TWO distinct records.

PHASE 2: Cart UI & Header Counter (Storefront)

Header Cart Badge: Fix the cart icon counter in the header. It must display the exact number of unique records (variants) in the cart list, NOT the total sum of all item quantities. (e.g., 2 Red items + 1 Green item = Cart Icon shows "2").

Cart List Item Refactor: Analyze cart-list-item.html and cart-list-item-unit.html.

Add variant indicators to the UI: Render a small circular div with the exact background color of the selected variant right next to the variant name.

For unit-supported products, the "Add/Edit" button must trigger a Bottom Sheet for unit selection. Include the UI state for the "Delete" button if they want to remove it entirely.

PHASE 3: Seller Dashboard (Add Product Form)

Discount Logic: Change the discount input field. It must now accept a Discount Percentage (%), not a flat price amount.

Minimum Order & Units: Implement the conditional Minimum Order logic. If the product has "Units" enabled, the minimum order input must be calculated per unit.

Storefront Counter Initialization: When a customer clicks "Add to Cart" on a product with a minimum order (e.g., min 3), the counter must immediately start at 3. The default fallback is 1.

PHASE 4: File Generation & Synchronization
Using the base assets located at C:\Sanji\Manooch-Deps\manooch-planings\cart-flow\modules\cart and \billing, generate the final HTML and Tailwind CSS files.

Ensure strict adherence to our RTL (dir="rtl") design system.

Write clean, semantic HTML with extensive Tailwind classes matching the manooch-fronts\packages design specs.

Save the final, unified prototypes into C:\Sanji\Manooch-Deps\manooch-planings\cart-flow\final.

Do not hallucinate layouts. Ensure the Sticky CTA, Cart List Items, and Dashboard inputs perfectly reflect these new business rules. Let me know when the files are generated.